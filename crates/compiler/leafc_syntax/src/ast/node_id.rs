/// A **unique** identifier for a node in the syntax tree.
pub struct NodeId(u32);

// EXAMPLE:
// use doc_comment::doc_comment;

// doc_comment!(include_str!("../../SYNTAX.md"), pub struct Foo(u32););
